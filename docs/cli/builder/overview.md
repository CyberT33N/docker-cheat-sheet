# docker builder

Manage builds.

> `docker builder` is backed by Buildx. `docker builder build` and `docker builder prune` map to the corresponding `docker buildx` commands.

## Usage

```text
docker builder COMMAND
```

## Subcommands

See [TOC](./toc.md) for the complete subcommand list.
