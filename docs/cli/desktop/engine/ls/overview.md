# docker desktop engine ls

List available engine modes (Windows only).

## Usage

```text
docker desktop engine ls
```

## Options

| Option | Description |
|---|---|
| `--format string` | Format the output. Accepted values are: pretty, json (default "pretty") |
