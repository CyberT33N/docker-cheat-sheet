# docker desktop engine use

Docker Desktop for Windows supports both Windows and Linux containers. This command lets you switch between Windows and Linux mode from the command line.

## Usage

```text
docker desktop engine use [MODE]
```

## Options

This command has no additional options.
