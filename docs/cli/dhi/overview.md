# docker dhi

Command-line interface for administering Docker Hardened Images. It provides commands to browse the DHI catalog, mirror images into your organisations and manage customizations.

> `docker dhi` is a Docker CLI plugin.

## Usage

```text
docker dhi [OPTIONS] COMMAND
```

## Options

| Option | Description |
|---|---|
| `-v, --version` | version for dhi |

## Subcommands

See [TOC](./toc.md) for the complete subcommand list.
