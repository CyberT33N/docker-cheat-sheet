# docker image load

Load an image from a tar archive or STDIN.

## Usage

```text
docker image load [OPTIONS]
```

## Aliases

- `docker load`

## Options

Identical to the canonical command. See [docker load](../../../load/overview.md) for the complete option reference.
