# docker manifest rm

Delete one or more manifest lists from local storage.

> EXPERIMENTAL: `docker manifest rm` is an experimental feature.

## Usage

```text
docker manifest rm MANIFEST_LIST [MANIFEST_LIST...]
```

## Options

This command has no additional options.
