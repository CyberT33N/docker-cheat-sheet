# docker offload status

Show the status of the Docker Offload connection.

## Usage

```text
docker offload status
```

## Options

| Option | Description |
|---|---|
| `-f, --format string` | Format of output (default: pretty, one of: pretty\|json) |
| `-w, --watch` | Watch for status updates |
