# docker buildx prune

Remove build cache.

## Usage

```text
docker buildx prune
```

## Aliases

- `docker builder prune`

## Options

Identical to the canonical command. See [docker builder prune](../../../builder/prune/overview.md) for the complete option reference.
