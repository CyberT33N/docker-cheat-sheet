# docker buildx build

Start a build.

## Usage

```text
docker buildx build [OPTIONS] PATH | URL | -
```

## Aliases

- `docker build`
- `docker builder build`
- `docker image build`
- `docker buildx b`

## Options

Identical to the canonical command. See [docker build](../../../build/overview.md) for the complete option reference.
