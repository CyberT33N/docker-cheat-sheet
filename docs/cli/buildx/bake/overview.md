# docker buildx bake

Build from a file.

## Usage

```text
docker buildx bake [OPTIONS] [TARGET...]
```

## Aliases

- `docker bake`
- `docker buildx f`

## Options

Identical to the canonical command. See [docker bake](../../../bake/overview.md) for the complete option reference.
