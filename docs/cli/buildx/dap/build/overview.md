# docker buildx dap build

Start a build (via debug adapter protocol).

## Usage

```text
docker buildx dap build [OPTIONS] PATH | URL | -
```

## Options

Accepts the same build options as the canonical command. See [docker build](../../../../build/overview.md) for the complete option reference.
