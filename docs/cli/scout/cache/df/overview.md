# docker scout cache df

Show Docker Scout disk usage. Shows temporary or cached data used by Docker Scout. Once generated, SBOMs are cached locally so they are not generated or fetched again.

## Usage

```text
docker scout cache df
```

## Options

This command has no additional options.
