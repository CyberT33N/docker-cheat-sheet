# docker model context use

Set the active Model Runner context. Pass "default" to revert to automatic backend detection.

## Usage

```text
docker model context use NAME
```

## Options

This command has no additional options.
