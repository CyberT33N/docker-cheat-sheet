# docker model context rm

Remove one or more Model Runner contexts.

## Usage

```text
docker model context rm NAME [NAME...]
```

## Aliases

- `docker model context remove`

## Options

This command has no additional options.
