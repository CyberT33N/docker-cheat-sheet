# docker model unload

Unload running models.

## Usage

```text
docker model unload (MODEL [MODEL ...] [--backend BACKEND] | --all)
```

## Aliases

- `docker model stop`

## Options

| Option | Description |
|---|---|
| `--all` | Unload all running models |
| `--backend string` | Optional backend to target |
