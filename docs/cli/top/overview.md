# docker top

Display the running processes of a container.

## Usage

```text
docker top CONTAINER [ps OPTIONS]
```

## Aliases

- `docker container top`

## Options

This command has no additional options. It accepts `ps` options as arguments.
