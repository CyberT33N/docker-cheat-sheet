# docker pass set

Set a secret.

## Usage

```text
docker pass set id[=value]
```

## Options

| Option | Description |
|---|---|
| `-f, --force` | Overwrite existing secret if it already exists |
| `--metadata stringArray` | Non-sensitive key=value metadata (repeatable) |
