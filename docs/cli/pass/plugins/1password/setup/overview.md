# docker pass plugins 1password setup

Set the 1Password service account token and enable the plugin.

## Usage

```text
docker pass plugins 1password setup
```

## Options

This command has no additional options. The token is provided via STDIN.
