# docker pass run

Run a command with `se://` environment references resolved.

## Usage

```text
docker pass run -- CMD [ARGS...]
```

## Options

| Option | Description |
|---|---|
| `--env-file stringArray` | Read environment variables from a dotenv-formatted file. Repeatable; later files override earlier files and the process environment. |
