# docker mcp oauth authorize

Authorize the specified OAuth app.

## Usage

```text
docker mcp oauth authorize <app>
```

## Options

| Option | Description |
|---|---|
| `--open-browser` | Open the authorization URL in the default browser automatically |
| `--scopes string` | OAuth scopes to request (space-separated) |
