# docker mcp catalog server

Manage servers in catalogs.

## Usage

```text
docker mcp catalog server COMMAND
```

## Subcommands

The subcommand surface is managed by the Docker MCP plugin and will be documented after verification.
