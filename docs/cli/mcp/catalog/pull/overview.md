# docker mcp catalog pull

Pull and import an MCP server catalog from an OCI registry.

## Usage

```text
docker mcp catalog pull <oci-reference>
```

## Options

This command has no additional options.
