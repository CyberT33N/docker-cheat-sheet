# docker mcp secret set

Set a secret in the local OS Keychain.

## Usage

```text
docker mcp secret set key[=value]
```

## Options

This command has no additional options. The value can be passed via STDIN.
