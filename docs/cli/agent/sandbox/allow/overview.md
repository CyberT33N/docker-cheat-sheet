# docker agent sandbox allow

Add host(s) to the persistent sandbox network allowlist.

## Usage

```text
docker agent sandbox allow <host> [<host>...]
```

## Options

Only the [global flags](../../overview.md) apply.
