# docker agent debug skills

Debug the skills of an agent.

## Usage

```text
docker agent debug skills <agent-file>|<registry-ref>
```

## Options

Only the [global flags](../../overview.md) and the debug group options apply.
