# docker agent debug title

Generate a session title from a question.

## Usage

```text
docker agent debug title <agent-file>|<registry-ref> <question>
```

## Options

| Option | Description |
|---|---|
| `--model stringArray` | Override agent model: [agent=]provider/model (repeatable) |

Plus the [global flags](../../overview.md) and the debug group options.
