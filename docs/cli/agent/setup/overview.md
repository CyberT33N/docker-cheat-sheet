# docker agent setup

Interactively set up a model (built-in provider, local, custom endpoint, or Claude Code).

## Usage

```text
docker agent setup
```

## Options

Only the [global flags](../overview.md) apply.
