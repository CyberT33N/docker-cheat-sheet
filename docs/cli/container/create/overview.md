# docker container create

Create a new container.

## Usage

```text
docker container create [OPTIONS] IMAGE [COMMAND] [ARG...]
```

## Aliases

- `docker create`

## Options

Identical to the canonical command. See [docker create](../../../create/overview.md) for the complete option reference (grouped into compute, networking, storage, lifecycle and system flags).
