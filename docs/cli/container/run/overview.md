# docker container run

Create and run a new container from an image.

## Usage

```text
docker container run [OPTIONS] IMAGE [COMMAND] [ARG...]
```

## Aliases

- `docker run`

## Options

Identical to the canonical command. See [docker run](../../../run/overview.md) for the complete option reference (grouped into compute, networking, storage, lifecycle and system flags).
