# docker container wait

Block until one or more containers stop, then print their exit codes.

## Usage

```text
docker container wait CONTAINER [CONTAINER...]
```

## Aliases

- `docker wait`

## Options

Identical to the canonical command. See [docker wait](../../../wait/overview.md) for the complete option reference.
